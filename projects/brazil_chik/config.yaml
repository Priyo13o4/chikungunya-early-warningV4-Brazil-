adapter:
  load_data: src.data_preprocessing.load_data:run
  label_outbreaks: projects.brazil_chik.label_adapter:label_outbreaks
  build_feature_matrix: projects.brazil_chik.feature_adapter:build_feature_matrix
  build_fold_ledger: projects.brazil_chik.cv_adapter:build_fold_ledger
  generate_time_splits: projects.brazil_chik.cv_adapter:generate_time_splits
  force_full_bayesian_project_only: false

bayesian_model:
  force_full_bayesian: false
  bayesian_simplified_mode: false
  max_convergence_retries: 0
  convergence_failure_mode: strict
